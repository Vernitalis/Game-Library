package com.example.gamelibrary;

/**
 * Klasa uruchamiająca aplikację.
 * Służy do wywołania głównej metody aplikacji {@link MainApp#main(String[])}.
 */
public class Launcher {

    /**
     * Punkt wejścia do aplikacji.
     * Wywołuje metodę {@link MainApp#main(String[])} w celu uruchomienia aplikacji.
     *
     * @param args Argumenty wiersza poleceń
     */
    public static void main(String[] args) {
        MainApp.main(args);
    }
}
